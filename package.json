{
  "scripts": {
    "build:slides": "marp slides.md -o docs/slides.html",
    "build:code": "node build-code.js",
    "build": "npm run build:code && npm run build:slides"
  },
  "dependencies": {
    "@marp-team/marp-cli": "0.21.0",
    "hubdown": "2.6.0"
  }
}
